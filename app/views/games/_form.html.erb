<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table border=1>
    <tr>
      <th>Deck</th>
      <th>opponent</th>
      <th>win?</th>
    </tr>
    <tr>
      <td>
        <%= f.text_field :deck %>
      </td>
      <td>
        <%= f.select :opponent_class, Game::OPPONENTS %>
        <%= f.label :did_i_win %><br>
      </td>
      <td>
        <%= f.check_box :did_i_win %>
      </td>
      <td>
        <%= f.submit %>
      </td>
    </tr>
  </table>
  <% end %>
