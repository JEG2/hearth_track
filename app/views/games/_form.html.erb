<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deck %><br>
    <%= f.text_field :deck %>
  </div>
  <div class="field">
    <%= f.label :opponent_class %><br>
    <%= f.text_field :opponent_class %>
  </div>
  <div class="field">
    <%= f.label :did_i_win %><br>
    <%= f.check_box :did_i_win %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
